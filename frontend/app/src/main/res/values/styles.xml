<resources>

    <style name="Widget.Theme.Pantry_Parser.ActionBar.Fullscreen" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/red</item>
    </style>

    <style name="Widget.Theme.Pantry_Parser.ButtonBar.Fullscreen" parent="">
        <item name="android:background">@color/red</item>
        <item name="android:buttonBarStyle">?android:attr/buttonBarStyle</item>
    </style>

    <style name="buttons" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/light_blue</item>
    </style>

    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/light_blue</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/light_blue
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/light_blue
        </item>
    </style>

    <style name="ActionBarThemeOverlay">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlHighlight">@color/black</item>
        <item name="android:textColorHint">@color/cardview_shadow_start_color</item>
    </style>

</resources>